<section class="outer-container">
  <h2 class="page-title">All posts in the <%= @category.name.downcase %> category</h2>
  <div class="list-grid">
    <ul>
      <%= render @posts %>
    </ul>
  </div>
</section>
<%= paginate @posts %>
