<h2 class="page-title">Welcome, please sign up!</h2>
<div class="form">
  <%= form_for @user do |form| %>
    <%= form.label :email %>
    <%= form.email_field :email %>
    <%= form.label :password %>
    <%= form.password_field :password %>
    <%= form.submit "Sign up" %>
  <% end %>
</div>
